module.exports = {
    asFaucet: function() {
        var connection = {}
        return Promise.resolve(connection);
    },

    asTank: function() {
        var connection = {}
        return Promise.resolve(connection);
    }
}
